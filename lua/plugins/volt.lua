return {
  "nvzone/volt",
  lazy = true,
  -- Uncomment one of these loading triggers based on your needs:
  -- cmd = { "VoltCommand" }, -- if volt provides specific commands
  -- event = "VeryLazy", -- load after Neovim startup is complete
  -- keys = { -- if you want specific keybindings
  --   { "<leader>v", "<cmd>VoltToggle<cr>", desc = "Toggle Volt" },
  -- },
}
