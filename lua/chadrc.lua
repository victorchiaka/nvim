local M = {}

M.colorify = {
	enable = false
}

M.tabufline = {
	enabled = true,
	lazyload = false,
	order = { "treeOffset", "buffers", "tabs", "btns" },
	modules = nil,
	-- bufwidth = 21,
}

return M
